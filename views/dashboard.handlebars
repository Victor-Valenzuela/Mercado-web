<h1 class='text-center pt-3'>Bienvenido al mercado WEB seleccione sus productos</h1>
<div>
    <div class="container">
        <div class="row">
            {{#each productos}}
            {{> producto p=this}}
            {{/each}}
        </div>
    </div>
    {{> modal}}
</div>